var $courses = $('#courses');
var $coursesCount = $('#courses-count');

$courses.html('<%= j(render partial: "courses").strip.html_safe %>');
$coursesCount.html('<%= j(render partial: "count").strip.html_safe %>');

var $dropdownMenu = $('.dropdown.menu', $courses);
$dropdownMenu.foundation();
