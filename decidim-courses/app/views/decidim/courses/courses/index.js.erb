var $grid = $('#courses');
var $assembliesCount = $('#courses-count');

$grid.find('.card-grid').html('<%= j(render(courses)).strip.html_safe %>');
$assembliesCount.html('<%= j(render partial: "count").strip.html_safe %>');
