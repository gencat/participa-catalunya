<p><%= t("devise.mailer.invitation_instructions.hello", email: @user.name) %></p>

<p>
  <%= t ".invited_you_to_join_a_course", invited_by: @invited_by.name, application: @user.organization.name %>
</p>

<p>
  <%= link_to t(".decline", course_title: translated_attribute(@course.title)),routes.decline_invitation_course_registration_type_course_registration_path(course_slug: @course.slug, registration_type_id: @registration_type.id) %>
</p>
<p>
  <%= link_to t(".registration", course_title: translated_attribute(@course.title)),routes.course_registration_type_course_registration_url(course_slug: @course.slug, registration_type_id: @registration_type.id) %>
</p>
