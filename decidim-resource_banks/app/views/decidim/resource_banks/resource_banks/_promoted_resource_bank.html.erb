<div class="card card--full card--process">
  <div class="row collapse card--process__row">
    <div class="columns mediumlarge-8 large-6 card--process__column">
      <div class="card__content">
        <%= link_to resource_bank_path(promoted_resource_bank), class: "card__link" do %>
          <h2 class="card__title"><%= translated_attribute promoted_resource_bank.title %></h2>
        <% end %>
        <%= decidim_sanitize html_truncate(translated_attribute(promoted_resource_bank.text), length: 630, separator: "...") %>
        <%= link_to resource_bank_path(promoted_resource_bank), class: "button small hollow card__button" do %>
          <%= t("resource_banks.promoted_resource_bank.more_info", scope: "layouts.decidim") %>
        <% end %>
      </div>
    </div>
    <div class="columns mediumlarge-8 large-6 card--process__column">
      <div class="card--full__image"
           style="background-image:url(<%= promoted_resource_bank.hero_image.url %>)">
        <div class="card__content row collapse">
          <div class="large-6 large-offset-6 columns">
            <%= link_to resource_bank_path(promoted_resource_bank), class: "button expanded button--sc" do %>
              <%= t("resource_banks.promoted_resource_bank.more_info", scope: "layouts.decidim") %>
            <% end %>
          </div>
        </div>
      </div>
    </div>
  </div>
</div>
